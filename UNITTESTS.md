# AccessLab — Test Coverage Plan

A batched, ordered plan for building out test coverage. We work **root → branches**:
shared foundations first (every other layer depends on them), then per-component
verticals, then shared inspector UI, then full user journeys. It covers both **unit
tests** (vitest) and **user end-to-end tests** (Playwright — proposed, see Infrastructure).

This is a living document. We tick batches off in the Progress tracker at the bottom.

---

## How we work each batch (the loop)

For every batch below:

1. **You** manually exercise that area in the running app (`corepack pnpm dev`), using the
   batch's **Manual browser pass** checklist, and note any bugs / surprises.
2. **You tell me the section is done.** Mention any bugs found — we fix the code first, so
   we never write tests that pin buggy behaviour.
3. **I audit** any existing tests in that area first — are they still fit for purpose? Do
   they need rewriting against changed behaviour? — then write the new tests.
4. **I run** `corepack pnpm test:unit` / `test:nuxt` (and Playwright for Part D), iterate to
   green, and confirm the pre-commit gate (`lint` + `typecheck`) still passes.
5. We move to the next batch.

> Nothing is written until you've done the manual pass and given the go-ahead for that
> section.

---

## Conventions (from the existing tests — match these)

- **`unit` vitest project** — node environment, relative imports (`../../app/...`), pure
  functions only. Rich "why" docstrings at the top of each file (see
  `checkbox-aria-checked-redundant.test.ts`).
- **`nuxt` vitest project** — happy-dom, `~/` alias, `mountSuspended` + a small v-model
  wrapper component for anything that touches `defineModel`, `useI18n`, `useState`, `watch`,
  or other Nuxt auto-imports (see `checkbox-controls.test.ts`).
- **Characterisation tests** pin exact emitted markup byte-for-byte — the rendered HTML is
  load-bearing (axe audits it, rules reason about it, the code drawer shows it). See
  `form-input-renderers.test.ts`.
- No project-wide `prettier --write`; format only files we touch.

---

## Test infrastructure status

- **vitest projects** are configured in `vitest.config.ts`: `unit` (node) + `nuxt`
  (happy-dom). Coverage via v8 is enabled. Scripts: `test`, `test:unit`, `test:nuxt`,
  `test:watch`, `test:coverage`.
- **Existing test files (6)** — audited in their respective batches:
  - `test/unit/example.test.ts` — placeholder; **delete** once a real unit file lands.
  - `test/unit/render-utils.test.ts` — 5 utils (Batch A1).
  - `test/unit/issue-formatting.test.ts` — 4 helpers (Batch A1).
  - `test/unit/form-input-renderers.test.ts` — 4 renderers (Batches B7–B10).
  - `test/unit/checkbox-aria-checked-redundant.test.ts` — 1 rule (Batch B8).
  - `test/nuxt/checkbox-controls.test.ts` — controls watcher (Batch B8).
- **End-to-end is NOT set up yet.** Proposed harness: `@playwright/test` +
  `@axe-core/playwright`, run against `corepack pnpm preview` (or `dev`). This needs install +
  config + a `test:e2e` script — that's **Batch D0**, and I'll confirm the tooling choice
  with you before installing anything.
- **Doc drift to fix at the end:** `PROGRESS.md` and `NOTES.md` both say the test suite is
  "scaffolding only / empty". That's no longer true — update them once coverage lands.

---

## Part A — Shared foundations

These underpin everything. Do them first; they're fast, deterministic, mostly node-env.

### A1 — Pure utils & formatting helpers `[unit / node]`

- **Files:** `app/utils/escapeHtml.ts`, `formatCssLength.ts`, `valueFromLabel.ts`,
  `inlineStyleAttribute.ts`, `associateLabel.ts`, `issueFormatting.ts`, `prettifyCss.ts`,
  `prettifyHtml.ts`, `domIds.ts`, plus the Learn-vocabulary drift guard
  (`learnConcepts.ts` / `learnCategories.ts` vs the Zod enums in `content.config.ts`).
- **Manual browser pass:** open the **code drawer** (bottom of preview) on any component —
  flip between HTML / CSS panes and use **Copy inline** / **Copy with classes**. Confirm the
  markup is escaped and pretty-printed correctly, and that the Issues panel rule titles read
  well (these exercise `prettifyHtml/Css`, `escapeHtml`, `issueFormatting`).
- **Audit existing:** `render-utils.test.ts` (5 utils) and `issue-formatting.test.ts` (4
  helpers) — confirm still accurate, then **extend**: `prettifyCss`/`prettifyHtml` have no
  tests yet (note `prettifyHtml` needs `DOMParser`, so its real coverage belongs in the
  `nuxt`/happy-dom project; the node fallback path can stay in `unit`).
- **New assertions:** pretty-printer wrapping thresholds + void-tag handling + attr escaping;
  a **drift-guard test** asserting `LEARN_CONCEPT_IDS` and the category list exactly match the
  `content.config.ts` Zod enums (catches the "files commonly touched together" hazard).

### A2 — Contrast `[unit / node + nuxt]`

- **Files:** `app/composables/useContrast.ts` (pure math via `export const _internal`), and
  the `ContrastBadge.vue` component.
- **Manual browser pass:** in **Controls → Colours**, change background / text / border
  colours and watch the **ContrastBadge** ratio + verdict (AAA / AA / AALarge / Fail). Try a
  translucent `rgba(...)`, a `#abc` short hex, identical fg/bg, and a large font size — check
  the verdict bucket flips where expected.
- **New assertions (node):** `_internal.parseColor` (hex 3/4/6/8, rgb/rgba, garbage→null),
  `relativeLuminance`/`wcag2ContrastRatio` against known pairs (black/white = 21:1),
  `flatten` alpha compositing, `isLargeText` boundaries (24px / 18.67px bold),
  `bucketVerdict` thresholds. **High value** — this math is reused by `placeholderContrast`
  and `focusLowContrast` rules.
- **New assertions (nuxt):** `ContrastBadge` maps verdict → label/icon/colour correctly.

### A3 — Unit conversion `[unit / nuxt]`

- **Files:** `app/composables/useUnitConversion.ts` (uses `useState`, so nuxt project).
- **Manual browser pass:** on a sized control (e.g. width / padding / font-size), toggle the
  **px ↔ rem** unit switch and confirm the value converts sensibly. Then drag the global
  **rem-baseline slider** (simulated root font-size) and confirm rem-based values re-resolve
  in the preview against the new root.
- **New assertions:** px↔rem round-trips; the **slider-reference (16px) vs simulated-root**
  distinction (`lengthToPx`/`fromPx` follow `simulatedRootPx`; `lengthToSliderPx`/
  `fromSliderPx` use the fixed 16); `resolveProps` flattens only `CssLength` values;
  `hasRem`; `isCssLength`; `convertLength` no-op when units match.

### A4 — Shared rules `[unit / node]`

- **Files:** `app/rules/shared/vague-label.ts`, `invisible-text.ts`, `overflow.ts` (DomRule).
- **Manual browser pass:** on any button, set the label to `"OK"` / `"Click here"` → expect
  a **vague-label** warning in Issues; set text colour == background → **invisible-text**
  critical; shrink width/height while keeping a long label → **content-overflow** critical.
  Confirm each clears when you undo it.
- **New assertions:** vague-label vocabulary hits + verb-noun passes + icon-mode skip +
  aria-label-over-label precedence; invisible-text exact-match only (case/space normalised);
  overflow X/Y/both branches and the no-overflow null.

### A5 — Audit pipeline & shared composables `[unit / nuxt]`

- **Files:** `useCustomRules.ts`, `useDomRules.ts`, `useAxeResults.ts` (`useAllViolations`,
  `useAxeCounts`), `useModelUpdater.ts`, `useToggleableSection.ts`, `useInlineToClass.ts`,
  `useManualReview.ts`.
- **Manual browser pass:** watch the **toolbar count badges** (critical / warnings / passing)
  while toggling props that trip rules — confirm criticals = serious+critical and warnings =
  moderate+minor. Toggle a control **section on/off** (e.g. font-size switch, focus ring) and
  confirm the section's enabled state tracks the model. Check **Copy with classes** output
  (`useInlineToClass`). Tick items in **Manual review** and watch the progress percentage.
- **New assertions:** `useCustomRules.evaluate` translates `ViolationResult`→`AxeResult` and
  only pushes non-null; `useDomRules` re-evaluates on measurement change and clears on null;
  `useAxeCounts` bucketing across all three sources; `useAllViolations` merge order;
  `useModelUpdater.update`/`updateMany` direct-mutation; `useToggleableSection.enabled`
  - `toggle`; `useInlineToClass.convert` (with/without other attrs, no-style→null);
    `useManualReview` axe+static merge, `setChecked`/`checkAll`/`uncheckAll`, progress maths
    (incl. empty = 100%).

---

## Part B — Per component (vertical slices)

For each component we cover **three layers together** because that's how you'll test it in the
browser: the **render function** (characterisation, node), its **custom rule evaluators**
(node), and its **controls watcher(s) / model logic** (nuxt). Recommended order runs simplest
→ most stateful.

**Per-component Manual browser pass (template):** cycle every **variant** in the picker;
exercise every **control**; toggle each **context wrapper**; click the preview to confirm the
**toast / state** behaviour; read the **code drawer** HTML/CSS/JS panes; and walk the
**Issues** list, tripping and clearing each custom rule.

| Batch   | Component                     | Render fn                                                                                   | Custom rules                                                                                                    | Controls / watcher                                                          |
| ------- | ----------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| **B1**  | `buttons-action-triggers`     | `buttons/shared/render.ts` (characterise the `<button>` / `type="button"` paths)            | `target-size` (AA+AAA), `focus-visible` (`focusNotVisible`+`focusLowContrast`), `focusable-in-anchor`           | `ActionTriggerControls.vue`                                                 |
| **B2**  | `buttons-form-buttons`        | submit/reset across `<button>`/`<input>` + `input-image`                                    | (shared button rules)                                                                                           | label-restore + form-wrap-by-default + contentType-clear watchers           |
| **B3**  | `buttons-toggle-buttons`      | toggle markup + pressed CSS                                                                 | `toggle-state-missing`, `toggle-wrong-attribute`                                                                | toggle controls                                                             |
| **B4**  | `buttons-switches`            | pill/thumb CSS + 3 variants + JS pane                                                       | `switch-no-role`, `switch-wrong-attribute` (both skip `input-checkbox-switch`)                                  | switch state + dark-mode demo (`onclick` → shell `toggleDarkMode`)          |
| **B5**  | `buttons-disclosure-triggers` | expanded/hidden panel                                                                       | `disclosure-no-state`, `disclosure-state-out-of-sync`                                                           | disclosure state                                                            |
| **B6**  | `buttons-menu-triggers`       | haspopup/expanded + hidden popup                                                            | `menu-no-haspopup`, `menu-no-expanded`                                                                          | menu state                                                                  |
| **B7**  | `input`                       | `input/render.ts` (**deepen** existing)                                                     | `input/aria-label-without-visible-label`, `placeholder-contrast`                                                | `InputControls.vue` + `useVariantLabelSync` (type→label)                    |
| **B8**  | `checkbox`                    | `checkbox/render.ts` (**audit + deepen**)                                                   | `group-no-fieldset`, `aria-checked-redundant` (**audit**), `checked-and-indeterminate`, `parent-child-mismatch` | `CheckboxControls.vue` (**audit** existing nuxt test) + `deriveParentState` |
| **B9**  | `radio`                       | `radio/render.ts` (**deepen**)                                                              | `group-no-fieldset`                                                                                             | `RadioControls.vue`                                                         |
| **B10** | `select`                      | `select/render.ts` (**deepen** — only 1 case today; add `select-multiple` + `div-combobox`) | `select-no-label`, `select-options-empty`, `select-not-keyboard`                                                | `SelectControls.vue`                                                        |

**Audits inside Part B:**

- **B7–B10** audit `form-input-renderers.test.ts` — it pins one variant per renderer; the
  others (input types, select-multiple, the div-combobox anti-pattern, checkbox group modes)
  are uncovered. Extend each toward full variant coverage.
- **B8** audits both `checkbox-aria-checked-redundant.test.ts` (rule) and
  `checkbox-controls.test.ts` (the `renderAs → ariaChecked` + parent-from-children watchers) —
  confirm they still match current behaviour after any bugfixes from your manual pass.

> The button-family renderer (`buttons/shared/render.ts`) is the ~500-line workhorse. I'll
> confirm it's import-clean for the node project (the form renderers already are) and pin
> output per `renderAs`, focus-ring CSS, and pressed/switch/disclosure/menu branches.

---

## Part C — Shared inspector UI & studio shell `[nuxt / mountSuspended]`

The outer branches — heavier component mounts. Do after the logic they present is covered.

- **C1 — Issues panel:** `IssueSection.vue` auto-expand on first violation (watch+immediate,
  _not_ `default-open`) and the "manual collapse stays collapsed" behaviour; `IssuesPanel.vue`
  composes 3 sections. _Manual:_ trip a rule and watch the section open once, then collapse it
  and trip another — it must stay shut.
- **C2 — Count badges:** `AnimatedCountBadge.vue` count display + reduced-motion. _Manual:_
  watch the toolbar badges tally.
- **C3 — Manual review panel:** `ManualReviewPanel.vue` checklist + progress badge wiring to
  `useManualReview`.
- **C4 — Learn panel & Read Mode:** `LearnPanel.vue` pinned-vs-categorised topic ordering;
  `useReadMode` open/switchTopic/close + return-path stash; `useInspectorTab.focusLearnTopic`.
  _Manual:_ click a `↗` Learn-link, confirm it opens the reader and "close" returns you to the
  same component.
- **C5 — Code drawer:** `CodeDrawer.vue` HTML/CSS/JS pane toggles, copy-confirm reset, the
  keyboard-operable resize handle, JS pane hidden when no `js`.
- **C6 — ComponentStudio shell:** teleport targets resolve; toast selection logic
  (`suppressDemoClickToast`, implicit-submit link only for type-less `<button>`); the
  wrapper-availability watcher clears a wrapper that a new variant disallows.

---

## Part D — User end-to-end (Playwright)

Real-browser journeys through the studio. The in-iframe axe audit already runs at runtime;
these tests drive the **user's** experience and add the planned **AAA gate on the app's own
chrome**.

- **D0 — Harness setup:** install `@playwright/test` + `@axe-core/playwright`, add config +
  `test:e2e` script, run against `corepack pnpm preview`. _(I'll confirm tooling with you
  first.)_
- **D1 — Studio core journey:** `/` redirects to action-triggers; sidebar nav switches
  components; preview iframe renders; controls survive navigation (the per-id `useState`).
- **D2 — Audit feedback loop:** change a control → preview repaints → Issues panel + toolbar
  counts update (e.g. shrink target size → critical appears).
- **D3 — Variant & wrapper pickers:** switching variant updates preview + Issues; wrapping in
  `<form>` changes submit behaviour.
- **D4 — Code drawer:** open, switch panes, Copy inline / Copy with classes write to clipboard.
- **D5 — Read Mode:** open a Learn topic from a `↗` link and from the Learn tab; deep-link
  `/learn/<topicId>` resolves SSR'd; close returns to the stashed studio route.
- **D6 — Global prefs persistence:** font / size / high-contrast / theme persist across reload
  (localStorage + color-mode cookie).
- **D7 — AAA gate (the big one):** run `@axe-core/playwright` against every studio + learn
  route and assert zero violations at WCAG 2.2 AAA. This is the gate AGENTS.md/PROGRESS.md call
  for but that was never wired. _Note: this audits the **app chrome**, not the user-tuned
  inspected component._
- **D8 — Mobile blocker:** below-`lg`, studio shows `MobileBlocker`; `/learn/*` stays usable.

---

## Progress tracker

| Batch | Area                                                                | Manual pass | Tests written | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----- | ------------------------------------------------------------------- | :---------: | :-----------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A1    | Pure utils & formatting                                             |      ◑      |       ◑       | Done: escapeHtml, formatCssLength, valueFromLabel, inlineStyleAttribute, associateLabel, issueFormatting, prettifyCss, prettifyHtml (unit fallback + nuxt DOM path), domIds. **Remaining:** Learn-vocab drift guard (learnConcepts/learnCategories vs content.config.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| A2    | Contrast (math + badge)                                             |      ☑      |       ☑       | `useContrast._internal` math + round-then-compare fix (`roundRatio`) + removed dead `passes`; `ContrastBadge` verdict→label + 2-dp ratio test (icon is `aria-hidden`/decorative, so the text label is the pinned cue).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| A3    | Unit conversion                                                     |      ☑      |       ☑       | `useUnitConversion` (nuxt): px↔rem, the **dual reference** (simulated-root vs fixed-16 slider), `convertLength`, `resolveProps` (rem demo → rules), `hasRem`/`isCssLength`, 2-dp round-trip contract. No bug — code confirmed correct.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| A4    | Shared rules                                                        |      ☑      |       ☑       | `vague-label` (effective-name/aria-override/icon), `content-overflow` (X/Y/both), `invisible-text` — **fixed** to compare parsed colours (catches `#fff`==`#ffffff`, the gap axe also skips), browser-verified end-to-end.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| A5    | Audit pipeline & shared composables                                 |      ☑      |       ☑       | `useCustomRules`/`useDomRules` translation + replace/clear, `useAllViolations` merge order, `useAxeCounts` bucketing (serious+critical/moderate+minor, axe-only passes), `useModelUpdater` direct-mutation, `useToggleableSection`, `useManualReview` progress (`useInlineToClass` already covered). No bugs found.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B1    | buttons-action-triggers                                             |      ☑      |       ☑       | `button-render.test.ts` (render: button vs button-button, name/icon/escaping, clean-markup-by-default, focus-ring CSS, border-color) + `button-shared-rules.test.ts` (target-size AA/AAA + no-double-report, focus-visible ×2, focusable-in-anchor; shared with B2). Controls panel is pure section composition — no watcher to unit-test. target-size converted to a DOM-measurement rule (`DomRule` reading `DomMeasurement.targetWidth/Height`, the control's offsetWidth/Height measured in the iframe) so an unsized button grades its real rendered size, not a phantom `0 × 0`; DOM rules are now declarable per-definition via `ComponentDefinition.domRules`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| B2    | buttons-form-buttons                                                |      ☑      |       ☑       | `form-button-render.test.ts` (naming by element: `<button>` content vs `<input>` value vs `<input type=image>` alt; value-prop ignored; image form data) + `form-button-controls.test.ts` (nuxt: contentType-clear, label-restore-vs-bespoke, form-wrap-by-default + non-form-wrapper-survives watchers).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| B3    | buttons-toggle-buttons                                              |      ☑      |       ☑       | `toggle-button-rules.test.ts` (unit: the axe division-of-labour — `toggle-state-missing` fills a gap axe can't see so must NOT defer; `toggle-wrong-attribute` duplicates axe so declares `supersededByAxe`) + `toggle-button-render.test.ts` (nuxt: pressed state shown 3 ways, JS-pane-matches-HTML, the bold-icon swap) + `toggle-button-controls.test.ts` (nuxt: `demo:click → togglePressed` cycle + the `none` guard). **Resolved:** `aria-checked` double-report — custom rule now defers to axe's `aria-allowed-attr` via the new `supersededByAxe` merge-layer suppression (covered in `audit-pipeline.test.ts`). Bold Lucide icon swapped in for icon content (scoped to this component).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B4    | buttons-switches                                                    |      ☑      |       ☑       | `switch-rules.test.ts` (unit: both rules are pure gap-fillers — plain button + valid aria-pressed are axe-silent — so NEITHER declares `supersededByAxe`, the contrast with toggle; both skip `input-checkbox-switch`) + `switch-render.test.ts` (nuxt: role=switch+aria-checked correct, aria-pressed wrong-attribute, the `none` works-visually-but-silent anti-pattern, the native variant, JS-pane-per-variant) + `switch-controls.test.ts` (nuxt: `demo:click → switchChecked` flip, the `none` gate, the always-active input-checkbox-switch exception). **Consistency:** dark-mode demo confirmed `onclick`-driven (not `:has()`); stale comment/NOTES/PROGRESS corrected + orphaned toast i18n removed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| B5    | buttons-disclosure-triggers                                         |      ☑      |       ☑       | `disclosure-render.test.ts` (wrapper marker + clean button, blank-line paragraphs, max-width wrap, aria-expanded/aria-controls per behaviour) + `disclosure-controls.test.ts` (nuxt: `demo:activate` toggles, none toggles, legacy `demo:click` ignored) + `disclosure-rules.test.ts` (both rules pure gap-fillers — neither declares `supersededByAxe` — and mutually exclusive). **Code:** pre-test cleanup (rename to `DisclosureTriggerControls`, panel text splits on blank lines not sentences, dedicated manual checklist, `disclosure-no-state` reworded for aria-controls) + migrated to the declarative `data-al-interaction` → `demo:activate` protocol (fixes the type-less `<button>` not revealing; bare button now toggles AND shows the missing-form lesson).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| B6    | buttons-menu-triggers                                               |      ☑      |       ☑       | `menu-render.test.ts` (wrapper marker + clean button, popup role=menu/menuitem, aria-haspopup/aria-expanded across the four behaviours, aria-controls) + `menu-controls.test.ts` (nuxt: `demo:activate` toggles `menuOpen`, none toggles, legacy `demo:click` ignored) + `menu-rules.test.ts` (both pure gap-fillers; OVERLAP on `none` where both fire). **Code:** migrated to the declarative protocol — reused the Chunk-1 shell branch with ZERO shell changes (the proof the protocol generalises).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| B7    | input                                                               |      ☑      |       ☑       | **Rules (all 4 tested):** `input-number-for-formatted-value` (type=number used for card/phone/postcode — intent heuristic on label/name, incl. `tel`) + `input-missing-autocomplete` (the _missing_ case axe never flags) + `placeholder-contrast` + `aria-label-without-visible-label` (incl. the search-icon moderate exception). **Controls/render:** added `labelAssociation: "title"` mode (surfaces axe `label-title-only`) and an Autocomplete control (surfaces axe `autocomplete-valid`); both verified live. **Watcher:** `use-variant-label-sync.test.ts` (type→label sync). `form-input-renderers` deepened (title-only + autocomplete emission).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| B8    | checkbox                                                            |      ☑      |       ☑       | **Rules:** `checkbox-rules.test.ts` (group-no-fieldset, checked-and-indeterminate [native-only], parent-child-mismatch [full derived matrix]) + audited `checkbox-aria-checked-redundant.test.ts`. **Render:** `checkbox-render.test.ts` deepens group-no-fieldset, the div name-from-content/none/mixed states, and the fixed "Options" legend. **Controls:** rewrote `checkbox-controls.test.ts` — added unmount cleanup + the previously-untested click bridge (`demo:click` cascade, `demo:click-child` parent auto-sync, and the override-preservation path) + the groupMode→derive watcher. **Bugfix:** children `<legend>` no longer borrows the `value` attribute — it's a fixed "Options". **Deferred div-checkbox shell migration: skipped** — the checkbox uses index-routed `demo:click-child`, not the host-owned-boolean `demo:activate`, so the protocol doesn't fit (same reasoning as Chunk 3).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| B9    | radio                                                               |      ☑      |       ☑       | **Rule:** `radio-rules.test.ts` (group-no-fieldset — gap-filler, no `supersededByAxe`). **Controls:** `radio-controls.test.ts` — `demo:click-child → selectedItem` mapping (+ out-of-range guard) and the group-items textarea parsing. **Bugfix:** `groupItemsText` setter now does a single combined write — the old two-write version raced through defineModel and dropped the list change when removing the currently-selected item (the removed item sprang back); the controls test guards it. **Defaults** refreshed to a nature theme ("What's your favourite habitat?" / Forest, Wetland, Grassland). Render already comprehensively covered in `form-input-renderers.test.ts` — no separate render file needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B10   | select                                                              |      ☑      |       ☑       | **Render:** `form-input-renderers` select cases deepened 1 → 12 — all three variants (`select-native`/`select-multiple`/`div-combobox`), four label-association modes, placeholder selected/deselected, empty options, the combobox `aria-controls` + `role=listbox`/`role=option` pairing, and the no-props reset path (byte-exact). **Rules:** `select-rules.test.ts` grades the three-way axe division of labour — `select-no-label` DUPLICATES axe (now declares `supersededByAxe: [select-name, aria-input-field-name]`), while `select-options-empty` + `select-not-keyboard` are pure gap-fillers (empty-but-valid select / un-runnable keyboard contract) that must NOT defer. **Controls:** `select-controls.test.ts` — the `select:change`/`combobox:select` pick bridge (valid/stale/malformed) + the Options textarea. **Bugfix:** the `optionsText` setter had the SAME defineModel double-write race radio had — removing the currently-selected option dropped the list edit (the removed option sprang back); single combined write fixes it, guard verified to fail on the old code. **Rule fix:** `select-no-label` now defers to axe (it was double-reporting the missing name axe already catches; siblings ride on axe alone). **div-combobox shell migration: skipped** — component-specific open/close/commit + per-option pick, not a host-owned boolean (same call as div-checkbox); future-migration note added to NOTES.md. |
| C1    | Issues panel                                                        |      ☑      |       ☑       | **IssueSection:** `issue-section.test.ts` pins the controlled-collapsible open-state contract — closed with no violations, auto-expands on the first violation (both immediate-on-mount and the 0→1 transition), a manual collapse STAYS collapsed when new violations arrive (the user-toggle lockout), and the passing section never auto-expands. **IssuesPanel:** `issues-panel.test.ts` pins the composition — always three sections (passing flagged `is-pass`) and the impact bucketing (critical+serious / moderate+minor / passes), agreeing with `useAxeCounts`. Open-state asserted via the trigger's `aria-expanded` + `.issue-card` presence; bucketing via child IssueSection `violations` props off a persistent reactive mount. No bugs found — pure characterisation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| C2    | Count badges                                                        |      ☑      |       ☑       | **AnimatedCountBadge:** `animated-count-badge.test.ts` pins the JS behaviour (the motion itself is CSS, neutralised by the `@media (prefers-reduced-motion: reduce)` block — there is no JS reduced-motion branch). **Display/tally:** renders count + the pre-translated noun; the FIRST count change snaps silently, every later change is debounced by `TALLY_SETTLE_MS` before the shown number ticks (so a flurry of audit re-runs settles to one tick). **Glow:** pulses `count-badge--glow` only for a NEW violation id, then clears after `GLOW_DURATION_MS`; suppressed on the initial ids population, for the success colour (`showsGlow` false), and when ids change without adding one (e.g. a removal). No bugs found — pure characterisation; noted the cosmetic edge case where a first user-caused increment snaps instead of tallying if the initial audit left the count unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| C3    | Manual review panel                                                 |      ☑      |       ☑       | **ManualReviewPanel:** `manual-review-panel.test.ts` covers the panel wiring over `useManualReview` (whose merge + progress maths are already pinned in `use-manual-review.test.ts`). **Shell:** an empty checklist shows only the empty message (no title/progress/controls); a populated one shows the title, the `{checked} of {total} reviewed` badge, Check-all/Clear-all buttons, and one item per check. **Merge:** axe `incomplete` items append AFTER the static checklist and are titled via `formatRuleId` (rule id → Title Case) while static titles stay verbatim. **Wiring:** Check-all/Clear-all move the progress badge, and `progressColor` maps 0% → neutral, part-done → info, 100% → success (asserted via the badge's `color` prop). No bugs — the panel reads its checklist once at setup, which is correct because both component pages key the route by `fullPath`, so the studio always remounts the panel with a fresh list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| C4    | Learn panel & Read Mode                                             |      ☑      |       ☑       | **useReadMode + focusLearnTopic:** `use-read-mode.test.ts` drives the real Nuxt router — `isOpen`/`activeTopicId` derive from the route; `open()` stashes the studio return path (synchronously) only when entering from the studio, and a second `open()` inside the reader does NOT overwrite it; `switchTopic()` replaces without touching the stash; `close()` restores the stash and clears it, falling back to `/` when nothing was stashed; `useInspectorTab.focusLearnTopic` forwards to `open()`. **LearnPanel:** `learn-panel.test.ts` pins the pinned-vs-categorised ordering — primary first, related in declaration order, primary deduped, unknown ids skipped; no pinned section without a primary/related; the category groups list below; clicking a card opens it in the reader. Nuxt Content (`useLearnTopics`/`useLearnTopicTree` via `queryCollection`) can't run in happy-dom, so those are mocked with controlled topics while the pinning inputs are real `useStudioToolbar` state. No bugs found — pure characterisation.                                                                                                                                                                                                                                                                                                                                                                                                     |
| C5    | Code drawer                                                         |      ☑      |       ☑       | **CodeDrawer:** `code-drawer.test.ts`. **Pane availability:** the CSS tab is disabled without CSS (enabled once it arrives) and the JS tab is hidden (`v-if`) without JS (shown once it arrives). **Switching:** clicking HTML/CSS/JS swaps the pane (read off the code region's `aria-label`); when the active pane loses its content the `hasCss`/`hasJs` watchers fall the view back to HTML. **Copy:** the copy button writes the active pane's content to the (mocked) clipboard, shows the Copied confirmation, then resets after `COPY_FEEDBACK_MS`; the CSS/JS tabs copy their own content. **Resize:** the `role=separator` handle is keyboard-operable — arrows step 24px, Home/End jump to the clamped bounds, reflected in `aria-valuenow`/`aria-valuemax`. ProsePre + UTooltip stubbed (Content highlighter / TooltipProvider deps a bare mount lacks), clipboard mocked, localStorage cleared + window height fixed per test. No bugs found — pure characterisation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| C6    | ComponentStudio shell                                               |      ☑      |       ☑       | **ComponentStudio:** `component-studio.test.ts`. Heavy shell, so the children are stubbed (the PreviewIframe stub exposes `render`), the six teleport targets are created by hand, and `useToast` is mocked. **Teleport resolution:** each panel + toolbar control lands in its inspector target. **Toast selection:** the generic `demo:click` toast fires normally but is suppressed when the definition sets `suppressDemoClickToast`; a `form:submitted` toast picks the right follow-up link — implicit-submit lesson for a type-less button, coordinates lesson for an image submit, NO link for an explicit submit — and renders the payload (`name=value`, or the no-payload label). **Wrapper-availability watcher:** clears a selected context wrapper the newly-chosen variant disallows, and keeps one that still applies. Live iframe render + real toasts/panels left to Part D. No bugs found — pure characterisation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| C7    | App shell (app.vue, error.vue)                                      |      ☑      |       ☑       | **App shell:** `app-vue.test.ts` + `error-vue.test.ts`. **app.vue** (mostly head/SEO config — no logic): one guard that `useHead`/`useSeoMeta` get lang=en + TRANSLATED title/description/ogTitle (not raw keys, not the literal `t(...)` string the ogTitle bug once shipped); both composables mocked to capture their args. **error.vue:** the component-slug detection — a 404 under `/components/` shows `Component "slug" is not in the registry` using the last path segment (incl. nested routes), with no such line outside `/components/` or for a non-404 (generic title instead), and the action button calls `clearError({ redirect })`. useError + clearError mocked; the path supplied via mountSuspended's `route` option. No bugs found — pure characterisation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| C8    | Toolbar pickers (VariantPicker, WrapperToggles)                     |      ☑      |       ☑       | **Toolbar pickers:** `variant-picker.test.ts` + `wrapper-toggles.test.ts`. **VariantPicker:** a single variant renders a plain label (no popover); several render the popover trigger with the selected label; clicking an option emits `update:modelValue` with the key; the selected option has `aria-current`; the Recommended badge renders; the `sections` computed groups CONSECUTIVE same-section variants (a section reappearing non-contiguously starts a fresh group — pinned via the separator count). **WrapperToggles:** single-select — choosing a wrapper emits `[key]` (replace), None emits `[]`; the trigger shows the selected label or None; the active option has `aria-current`; the learn link renders only when the selected wrapper declares a topic. Both mount standalone with props/emits; UPopover stubbed to expose the trigger + #content slots (USeparator stubbed to a marker for VariantPicker). No bugs found; both still use `defineProps`/`defineEmits` (VariantPicker is backlog item 10's last defineModel holdout — flagged, not changed).                                                                                                                                                                                                                                                                                                                                                                     |
| C9    | Preview iframe + axe audit                                          |      ☑      |       ☑       | **Preview iframe + axe audit:** `use-axe-audit.test.ts` + `preview-iframe.test.ts`. **useAxeAudit:** the source-filtered message handler — preview:ready→isReady, axe:result fills the three arrays (defensively coercing non-arrays to []), axe:error captures a string message (else null), overflow:result stores the DOM measurement; messages from the wrong `event.source` or with no `type` are ignored; resetState clears the shared state on mount. **PreviewIframe:** the render queue (queued before ready then flushed; immediate when ready) + the audit-error surface. **Fixes applied (agreed):** (1) the hardcoded 'Loading preview…' string is now `t('preview.loading')`; (2) the previously-dead `errorMessage` is now surfaced — a watch in PreviewIframe raises an error toast (`studio.toasts.axeError`) + a dev-only `console.error`, deduped (the watch fires only on change). No functional bugs found.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| C10   | Pages (index, [component], [pattern], learn/index, learn/[topicId]) |      ☑      |       ☑       | **Pages:** `page-component-routes.test.ts` (index.vue + components/[component].vue + buttons/[pattern].vue) + `page-learn-index.test.ts` + `page-learn-topic.test.ts`. **Studio routes:** index→default redirect; [component] maps slug→getDefinition (real registry + ComponentStudio stub), redirects the legacy `button` slug, and 404s an unknown one; [pattern] maps `buttons-<pattern>` and 404s unknown (createError mocked so the throw rejects with a statusCode). **learn/index:** the status-aware redirect — first topic→redirect, loaded-empty/error→`/` fallback, still-loading→wait. **learn/[topicId]:** the three render states (pending / not-found / article with frontmatter title); useAsyncData mocked, ContentRenderer stubbed. **Fixes applied (agreed):** (1) learn/index's hardcoded 'Loading…' is now `t('learn.readMode.loading')`; (2) the comment-promised `/` fallback is now actually implemented — `useLearnTopics`/`useLearnTopicTree` expose the useAsyncData `status`, and learn/index falls back to `/` only once the tree has loaded (success/error) and is empty. No other bugs — index/[component]/[pattern]/[topicId] were clean.                                                                                                                                                                                                                                                                             |
| C11   | Inspector tab + learn topics + preview focus                        |      ☑      |       ☑       | **Composables/utils:** `use-inspector-tab.test.ts` + `use-learn-topics.test.ts` + `focus-preview-content.test.ts`. **useInspectorTab:** activeTab defaults to controls + is writable; focusLearnTopic forwards the topic id to the reader's open() (mocked useReadMode). **useLearnTopics:** topics map raw docs → LearnTopic (related/concepts default to []), empty when data is null; useLearnTopicTree groups by category in LEARN_CATEGORIES order, sorts by order, drops empty categories; status forwarded by both (useAsyncData mocked). **focusPreviewContent:** first-focusable / mount-fallback / iframe-fallback / no-contentDocument / null-iframe, plus the disabled-fallthrough regression guard (verified to fail on the old selector). **Fixes applied (agreed):** (1) removed dead `focusPanel` + `setActive` from useInspectorTab (no callers); (2) focusPreviewContent's focusable selector now excludes `[disabled]` + hidden inputs, so a disabled inspected element falls through to focus the mount instead of no-op'ing. useLearnTopics clean.                                                                                                                                                                                                                                                                                                                                                                                |
| D0    | E2E harness setup                                                   |      ☐      |       ☐       | confirm tooling first                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D1    | Studio core journey                                                 |      ☐      |       ☐       |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D2    | Audit feedback loop                                                 |      ☐      |       ☐       |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D3    | Variant & wrapper pickers                                           |      ☐      |       ☐       |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D4    | Code drawer (e2e)                                                   |      ☐      |       ☐       |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D5    | Read Mode (e2e)                                                     |      ☐      |       ☐       |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D6    | Global prefs persistence                                            |      ☐      |       ☐       |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D7    | AAA gate (all routes)                                               |      ☐      |       ☐       |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D8    | Mobile blocker                                                      |      ☐      |       ☐       |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
